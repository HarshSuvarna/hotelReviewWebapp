document.addEventListener('DOMContentLoaded', () => {
  const developers = document.querySelector('.developers');
  setTimeout(() => {
    developers.style.color = '#007BFF'; // Change to a preferred color
  }, 2000); // Wait for 2 seconds before changing color
});
